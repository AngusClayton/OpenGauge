# Shared headers

This directory contains shared firmware declarations.

- `GaugeConfig.h` defines gauge profiles.
- `DataSources.h` defines OBD and analogue data sources.
- `ConfigManager.h` declares configuration access functions.
- `Sensors.h` declares sensor and G-meter access functions.

Keep hardware-specific implementation code in `src/`.
