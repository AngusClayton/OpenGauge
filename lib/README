# Project libraries

This directory is reserved for project-specific PlatformIO libraries.

There are no local libraries at present. Add a library in its own directory
when it is reusable and has a clear interface.
